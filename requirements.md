## Packages
framer-motion | Smooth scroll animations and reveal effects
react-scroll | Smooth scrolling for navigation links

## Notes
The CV PDF is located in `attached_assets/`. It should be moved to `client/public/resume.pdf` for the download button to work.
API endpoints correspond to `shared/routes.ts` structure.
